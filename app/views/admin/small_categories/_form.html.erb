<%= f.alert_message "Please fix the errors below." %>

<%= f.text_field :name %>
<%= f.select :big_category_id, BigCategory.all.map{|t| [t.name, t.id]} %>

<%= f.primary "Submit" %>
<%= link_to "Cancel", admin_small_categories_path, :class => 'btn btn-default' %>